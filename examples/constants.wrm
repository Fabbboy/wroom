const other = 69
pub let hell = 123 + other
pub const other2: float = 1.0 + hell

extern func puts(a: int) void 

func adder(a: int, b: int) int {
    return a + b
}

pub func main() int {
    const t = 1
    const val = adder(t, hell)
    puts(val)
    return val
}